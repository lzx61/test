服务端TCP、UDP均采用多线程方式。
UDP传输文件流程
1、客户端通过TCP发送get请求；
2、服务器端解析get请求，有该文件且不是目录，返回文件绝对路径与文件大小，否则返回错误信息；
3、客户端接收到文件绝对路径、大小，将绝对路径通过UDP发送给服务端；
4、服务端UDP收到数据包，启动UDP线程；
5、将文件分次读取到byte[]缓存中，每次封装一个DatagramPacket，依次发送给客户端；
6、客户端依次接收DatagramPacket，取出byte[]写入文件输出流中，写入当前目录，如果文件存在直接覆盖。